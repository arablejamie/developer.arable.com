// for index.styl. its job is to override the default styles of application.
// According to the priority principle of css, the later style has a higher priority
// so it should be generated at the end of the CSS file.

body{
  font-family "Metric", "Helvetica Neue", Arial, sans-serif;

  h1
  {
    font-size: 35px;
  }

  .nav-item
  {
    font-size: 16px;
  }

  .navbar
  {
    //background: #F74B1B;
  }

  .navbar .site-name 
  {
    font-size: 16px;
    font-weight: 600;
  }

  .maint-title
  {
    font-weight: 500;
    font-size: 45px;
  }
}


  
  